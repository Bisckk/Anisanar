---
export interface Props {
  title: string;
  description: string;
  canonical?: string;
  image?: string;
  imageAlt?: string;
  type?: 'website' | 'article';
  schema?: object;
  noindex?: boolean;
}

const {
  title,
  description,
  canonical,
  image = '/og-image.jpg',
  imageAlt = 'Anisanar Veterinaria — Cuidamos a tu mejor amigo',
  type = 'website',
  schema,
  noindex = false,
} = Astro.props;

const siteUrl   = 'https://anisanar.com';
const fullTitle = title ? (title.includes('Anisanar') ? title : `${title} | Anisanar Veterinaria`) : 'Anisanar Veterinaria';
const ogImage   = image.startsWith('http') ? image : `${siteUrl}${image}`;
const canonicalUrl = canonical ?? Astro.url.href;

// ─── JSON-LD Schemas ───────────────────────────────────────────────────────
const localBusinessSchema = {
  '@context': 'https://schema.org',
  '@type': ['LocalBusiness', 'VeterinaryCare'],
  '@id': siteUrl,
  name: 'Anisanar Veterinaria',
  description: 'Centro veterinario en San Gil, Santander (Colombia), especializado en medicina general, cirugía, diagnóstico por imagen, rehabilitación y nutrición clínica para perros y gatos.',
  url: siteUrl,
  telephone: '+57-310-312-0430',
  email: 'centroveterinarioanisanar@gmail.com',
  address: {
    '@type': 'PostalAddress',
    streetAddress: 'Cr 17 N 20-69, Avenida Santander',
    addressLocality: 'San Gil',
    addressRegion: 'Santander',
    addressCountry: 'CO',
  },
  geo: {
    '@type': 'GeoCoordinates',
    latitude: 6.5551475,
    longitude: -73.1415999,
  },
  openingHoursSpecification: [
    {
      '@type': 'OpeningHoursSpecification',
      dayOfWeek: ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'],
      opens: '07:00',
      closes: '19:00',
    },
  ],
  sameAs: [
    'https://www.instagram.com/veterinaria_anisanar',
    'https://www.facebook.com/share/14fMGzmUgKQ/',
    'https://www.tiktok.com/@centroveterinario26',
  ],
  image: ogImage,
  priceRange: '$$',
  currenciesAccepted: 'COP',
  paymentAccepted: 'Cash, Credit Card, Debit Card',
  areaServed: {
    '@type': 'City',
    name: 'San Gil',
  },
};

const webPageSchema = {
  '@context': 'https://schema.org',
  '@type': 'WebPage',
  '@id': canonicalUrl,
  url: canonicalUrl,
  name: fullTitle,
  description,
  isPartOf: { '@id': siteUrl },
  inLanguage: 'es-CO',
  dateModified: new Date().toISOString(),
};

const websiteSchema = {
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  '@id': `${siteUrl}/#website`,
  url: siteUrl,
  name: 'Anisanar Veterinaria',
  description: 'Centro veterinario de confianza en San Gil, Santander',
  potentialAction: {
    '@type': 'SearchAction',
    target: {
      '@type': 'EntryPoint',
      urlTemplate: `${siteUrl}/?q={search_term_string}`,
    },
    'query-input': 'required name=search_term_string',
  },
  inLanguage: 'es-CO',
};
---

<!-- Primary Meta Tags -->
<title>{fullTitle}</title>
<meta name="title"       content={fullTitle} />
<meta name="description" content={description} />
<meta name="robots"      content={noindex ? 'noindex, nofollow' : 'index, follow, max-snippet:-1, max-image-preview:large, max-video-preview:-1'} />
<meta name="author"      content="Anisanar Veterinaria" />
<link rel="canonical"    href={canonicalUrl} />

<!-- Open Graph -->
<meta property="og:type"        content={type} />
<meta property="og:url"         content={canonicalUrl} />
<meta property="og:title"       content={fullTitle} />
<meta property="og:description" content={description} />
<meta property="og:image"       content={ogImage} />
<meta property="og:image:alt"   content={imageAlt} />
<meta property="og:image:width" content="1200" />
<meta property="og:image:height"content="630" />
<meta property="og:locale"      content="es_CO" />
<meta property="og:site_name"   content="Anisanar Veterinaria" />

<!-- Twitter Cards -->
<meta name="twitter:card"        content="summary_large_image" />
<meta name="twitter:title"       content={fullTitle} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image"       content={ogImage} />
<meta name="twitter:image:alt"   content={imageAlt} />

<!-- Favicons + Icons -->
<link rel="icon"             type="image/png" href="/logo-anisanar.png" />
<link rel="icon"             type="image/png"     href="/icons/icon-192.png" sizes="192x192" />
<link rel="apple-touch-icon"                      href="/icons/icon-192.png" />
<link rel="manifest"                              href="/site.webmanifest" />
<meta name="theme-color"    content="#ffffff" />

<!-- JSON-LD -->
<script type="application/ld+json" set:html={JSON.stringify(localBusinessSchema)} />
<script type="application/ld+json" set:html={JSON.stringify(webPageSchema)} />
<script type="application/ld+json" set:html={JSON.stringify(websiteSchema)} />
{schema && <script type="application/ld+json" set:html={JSON.stringify(schema)} />}
